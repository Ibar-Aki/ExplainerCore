# PLANS — ロードマップと次アクション

> **このファイルは「今どこにいるか」と「次に何をするか」の唯一の場所です。**  
> 次アクションは README や他のドキュメントには書かず、ここだけで管理します。

---

## 現在地

フェーズ1〜3の初期実装は完了しています。現在は **フェーズ4（実運用）** の入口にいます。  
Remote採点の動作確認と、実セッションの蓄積が次の主な課題です。

---

## 全体ロードマップ

| フェーズ | 内容 | 状態 |
|---|---|---|
| **フェーズ1** 調査と評価設計 | 4能力の評価軸を定め、調査プロンプトを整備する | ✅ 完了 |
| **フェーズ2** 訓練設計と教材化 | シナリオとルーブリックをJSON化し、運用テンプレートを整える | ✅ 完了 |
| **フェーズ3** Webアプリ実装 | `coach-web` に訓練モジュール・録音・AI採点・履歴表示を統合する | ✅ 初期版完了 |
| **フェーズ4** 実運用と改善 | セッションを蓄積し、評価精度と訓練導線を継続改善する | 🚀 進行中 |

---

## 完了済み成果物

- [x] ルートディレクトリ構成と設計ドキュメント一式
- [x] deep research プロンプト 6本
- [x] ルーブリックJSON（4モジュール分）とシナリオJSON（複数難度）
- [x] `coach-web` のローカル実行骨格（フロント + APIサーバー）
- [x] Baseline 自己診断の保存機能
- [x] Provider切り替え（OpenAI / Claude / Gemini）と設定状態の表示
- [x] fallback評価ロジックとRemoteタイムアウト切り替え
- [x] 履歴 index と静的データキャッシュ
- [x] Evidence-Based Warmups（各モジュール冒頭）
- [x] 30 / 60 / 90秒プリセット、`successSignals`・`constraints` 表示
- [x] 自己認識差分サマリーと簡易メトリクス表示
- [x] `npm test` による回帰防止テスト

---

## 次にやること（優先順）

1. **Remote実通信の確認** — OpenAI / Claude / Gemini それぞれで1回ずつ採点を通す
2. **実セッションの蓄積** — fallback と Remote の `nextActions` 品質差を比較する
3. **ルーブリック・シナリオの調整** — 実運用で弱かった箇所に合わせて修正する
4. **調査結果の反映** — `docs/research/deep-research` の知見を訓練導線へ反映する

---

## 完了条件

- 情報収集・評価・訓練・履歴保存の役割分担が明確である
- Webアプリからベースライン診断と訓練セッションを回せる
- 訓練結果が `sessions` に残り、過去と比較できる
- 実装仕様とドキュメントの内容が一致している
